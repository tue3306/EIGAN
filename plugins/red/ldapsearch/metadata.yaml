# SCAFFOLD HONESTO (roadmap) — ver docs/ROADMAP.md.
name: ldapsearch
category: red
subcategory: network
capabilities: [ad_enumeration]
supported_perspectives: [internal]
tool: ldapsearch
roadmap: true
enabled_by_default: false
description: >-
  Consulta LDAP/AD (naming contexts, usuários, grupos) — assumed breach. Disparado
  quando o nmap acha LDAP (389/636/3268).
install_hint: "sudo apt install ldap-utils  ·  ldapsearch(1)"
impact_class: active_safe
