# ATIVO — runner + parser + testes implementados. Intrusivo (envia payloads XSS):
# só dentro do escopo autorizado. impact_class active_intrusive (Policy arbitra).
name: dalfox
category: red
subcategory: web
capabilities: [xss_validation]
supported_perspectives: [external, internal]
tool: dalfox
roadmap: false
enabled_by_default: true
description: >-
  Validação de XSS (refletido/DOM) por injeção de payloads e verificação real de
  execução (dalfox). Testa os parâmetros de uma URL descoberta no crawl.
version_source: "dalfox version  # VERIFICAR"
license: MIT
commercial_use: ok
install_hint: "https://github.com/hahwul/dalfox"
impact_class: active_intrusive
selection:
  speed: medium
  accuracy: high
  resource_usage: medium
