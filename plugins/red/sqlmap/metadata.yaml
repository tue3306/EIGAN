# ATIVO — runner + parser + testes implementados. Uso GATED (intrusivo): só dentro
# do escopo autorizado; impact_class exploit_validation (Policy Engine arbitra).
name: sqlmap
category: red
subcategory: web
capabilities: [exploitation]
supported_perspectives: [external, internal]
tool: sqlmap
roadmap: false
enabled_by_default: true
description: >-
  Detecção e validação autorizada de injeção SQL (sqlmap). Intrusivo — roda em modo
  não-destrutivo (--batch, sem dump/OS), só confirma o ponto de injeção e o DBMS.
version_source: "sqlmap --version  # VERIFICAR"
license: GPL-2.0
commercial_use: verify
install_hint: "sudo apt install sqlmap  ·  https://sqlmap.org/"
impact_class: exploit_validation
selection:
  speed: low
  accuracy: high
  resource_usage: medium
